server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ruptis/malurus-config-server
          searchPaths: '{application}'
          default-label: k8s-deploy
          timeout: 3
